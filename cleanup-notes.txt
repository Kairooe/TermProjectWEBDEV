Cleanup audit — 2026-04-05
==========================

1. FILES DELETED
----------------

client/src/App.css
  Reason: Vite scaffold CSS. Never imported by any component (grep confirmed).
  Contains styles for .counter, .hero, #center, #next-steps, #docs, #spacer, .ticks —
  none of these selectors appear in any React component.

client/src/assets/react.svg
client/src/assets/vite.svg
client/src/assets/hero.png
  Reason: Vite default scaffold assets. grep confirmed no import in any source file.

server/src/routes/.gitkeep
server/src/middleware/.gitkeep
  Reason: Empty placeholder files added to track directories in git.
  Both directories now contain real source files and no longer need placeholders.


2. INLINE DEAD CODE REMOVED
----------------------------

server/src/routes/ai.js — non-error console.log calls removed
  - Removed: console.log('[ai/generate] forwarding to Ollama — model:', ...)
  - Removed: console.log('[ai/generate] Ollama responded — status:', ..., '— bytes:', ...)
  Reason: Informational per-request logs, not error logs. Log every incoming request
  and every Ollama response, which is noisy in production.

PlatformIO/…/src/config.h — unused #define removed
  - Removed: #define BACKEND_URL  BACKEND_BASE "/api/study/record"
  Reason: Never referenced in any .cpp or .h file. BackendClient.cpp constructs
  its URL dynamically as String(_baseUrl) + "/api/device/record". This define was
  a leftover from an earlier iteration.

PlatformIO/…/src/OllamaClient.cpp line 7 — unused static array removed
  - Removed: static const char* const LABELS[4] = {"A", "B", "C", "D"};
  Reason: Declared at file scope but never referenced anywhere in OllamaClient.cpp.
  LEDController.cpp has its own identical LABELS array that IS used.

PlatformIO/…/src/OLEDDisplay.h — unused method declaration removed
PlatformIO/…/src/OLEDDisplay.cpp — unused method implementation removed
  - Removed: showSplash(const char* statusLine) (26 lines in .cpp, 1 line in .h)
  Reason: grep confirmed it is never called from main.cpp or any other file.
  The splash screen functionality was superseded by showStatus(), which is used
  throughout.


3. ITEMS FOUND BUT LEFT ALONE
------------------------------

client/src/index.css
  Reason: Imported by main.jsx. Contains legitimate base styles (body margin reset,
  #root layout, h1/h2 typography, code element style) alongside some Vite template
  CSS variables. Removing or trimming this file risks breaking the overall layout
  without running the app to verify.

server/server.js — console.log(`Server running on port ${PORT}`)
  Reason: Startup status message confirming the process bound successfully.
  Not a per-request debug log.

server/src/config/db.js — console.log(`MongoDB connected: ${conn.connection.host}`)
  Reason: Startup status message confirming the database connection. Not a
  per-request debug log.

server/server.log
  Reason: Runtime log file generated by the start command (node server.js >> server.log).
  Not a source file; already excluded from git via .gitignore.

PlatformIO/…/src/wifi_manager.h — _generatedPin (8-digit PIN) vs README (6-digit code)
  Note: The WiFiManager generates an 8-digit _generatedPin used as the SoftAP
  password for the captive-portal setup flow. The separate 6-digit deviceCode
  in main.cpp/DeviceClient is used for the game pairing flow. These are two
  different things; both are used.
